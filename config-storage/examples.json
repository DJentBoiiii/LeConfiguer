{
  "POST /configs - Create new config with YAML type": {
    "id": "app-config-1",
    "name": "Application Configuration",
    "type": "yaml",
    "environment": "production",
    "json_content": {
      "server": {
        "port": 8080,
        "host": "localhost"
      }
    },
    "tags": ["backend", "production", "v1"]
  },

  "POST /configs - Create new config with JSON type": {
    "id": "db-config-1",
    "name": "Database Configuration",
    "type": "json",
    "environment": "development",
    "json_content": {
      "host": "localhost",
      "port": 5432,
      "database": "mydb"
    },
    "tags": ["database", "postgres"]
  },

  "POST /configs - Create config without ID (auto-generated)": {
    "name": "Redis Configuration",
    "type": "toml",
    "environment": "staging",
    "json_content": {
      "redis": {
        "host": "localhost",
        "port": 6379
      }
    },
    "tags": ["cache", "redis"]
  },

  "POST /configs - Example from user": {
    "id": "ansible-1",
    "name": "Ansible 1 Config",
    "type": "yaml",
    "environment": "dev",
    "json_content": {
      "Python": "3.10"
    },
    "tags": []
  },

  "PUT /configs/{id} - Update existing config": {
    "name": "Updated Application Configuration",
    "type": "yaml",
    "environment": "production",
    "json_content": {
      "server": {
        "port": 9090,
        "host": "0.0.0.0",
        "timeout": 30
      }
    },
    "tags": ["backend", "production", "v2"]
  },

  "POST /configs - XML configuration": {
    "id": "xml-config-1",
    "name": "XML Configuration",
    "type": "xml",
    "environment": "production",
    "json_content": {
      "config": {
        "setting": "value"
      }
    },
    "tags": ["xml", "legacy"]
  }
}
